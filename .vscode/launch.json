{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/bullet-tracker-api/package.json",
                "localRoot": "${workspaceFolder}/bullet-tracker-api"
            }
        }
    ]
}